<div class="ui card" id ="{{name}}">
  <div class="image">
    <img src="{{image}}">
  </div>
  <div class="content">
    <a class="header" id = "headerName">{{fullname}}</a>
    <div class="description">
      {{name}} is a {{job}} based in {{city}}.
    </div>
  </div>
</div>